name: ram_app
description: A new Flutter application.

version: 1.0.0+1

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^0.1.2
  graphql_flutter: ^3.0.0
  provider: ^4.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:

  assets:
    - logo.png

  fonts:
    - family: GloriaHallelujah
      fonts:
        - asset: fonts/GloriaHallelujah-Regular.ttf

  uses-material-design: true

